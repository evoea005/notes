<IfModule mod_rewrite.c>
	Options -Multiviews
	RewriteEngine On
	RewriteBase /editor/public
	RewriteCond %{REQUEST_FILENAME} !-d
	RewriteCond %{REQUEST_FILENAME} !-f
	RewriteCond %{REQUEST_FILENAME}\.php -f
	RewriteRule ^(.*)$ $1.php [NC,L] 
</IfModule>